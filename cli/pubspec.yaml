name: fcnui
description: The fcnui CLI (Command-Line Interface) tool. It is used to generate component code for the fcnui package.
version: 1.0.6
repository: https://github.com/shoh-dev/flutter_cn_ui
homepage: https://fcnui.shoh.dev
documentation: https://fcnui.shoh.dev/docs

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  dio: ^5.4.0
  fpdart: ^1.1.0
  yaml_edit: ^2.1.1

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0

executables:
  fcnui: fcnui