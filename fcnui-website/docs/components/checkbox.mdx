# Checkbox

A control that allows the user to toggle between checked and not checked.

## Installation
```shell
fcnui add checkbox
```

> :warning: **Important**:
> When added, need to stop running app and run `flutter pub get` to install all necessary dependencies.


## Depends

Adds the following components to your project along with the input component:

- [Form](/docs/components/form)

## Examples

import DefaultIframe from '../../src/components/default_iframe'

### With Label
<DefaultIframe src="https://fcnui-playground.vercel.app/#/checkbox?variant=withLabel" height={400}/>

### Disabled
<DefaultIframe src="https://fcnui-playground.vercel.app/#/checkbox?variant=disabled" height={400}/>

### Card
<DefaultIframe src="https://fcnui-playground.vercel.app/#/checkbox?variant=card" height={400}/>

### Form
> [Docs](/docs/components/form)
<DefaultIframe src="https://fcnui-playground.vercel.app/#/checkbox?variant=form" height={600}/>