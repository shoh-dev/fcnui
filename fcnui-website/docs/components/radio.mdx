# Radio

A set of checkable buttons—known as radio buttons—where no more than one of the buttons can be checked at a time.

## Installation
```shell
fcnui add radio
```

> :warning: **Important**:
> When added, need to stop running app and run `flutter pub get` to install all necessary dependencies.


## Depends

Adds the following components to your project along with the input component:

- [Form](/docs/components/form)

## Examples

import DefaultIframe from '../../src/components/default_iframe'

### Default
<DefaultIframe src="https://fcnui-playground.vercel.app/#/radio" height={400}/>

### Form
> [Docs](/docs/components/form)
<DefaultIframe src="https://fcnui-playground.vercel.app/#/radio?variant=form" height={600}/>